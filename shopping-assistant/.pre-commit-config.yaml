repos:
  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v4.6.0
    hooks:
      - id: end-of-file-fixer
      - id: trailing-whitespace
  - repo: local
    hooks:
      - id: semgrep
        name: Semgrep Security Scan
        entry: shopping-assistant/.venv/bin/semgrep scan --config=shopping-assistant/.semgrep/rules.yaml --error
        language: system
        types: [python]
        pass_filenames: false
